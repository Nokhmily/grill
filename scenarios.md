# 场景施压库 — 8+2 个场景的完整推演内容

本文件包含所有场景施压的详细内容。**主文件 `skill.md` 只引用本文件的框架，具体推演内容在此文件中。**

使用规则：
- 🟢 轻量模式：只用场景 0 + 场景 1 + 场景 6
- 🟡 标准模式：用场景 0-6（跳过场景 7-8）
- 🔴 深度模式：全部 8+2 个场景

---

## 拷打第 0 步：Aha Moment 路径 — "用户什么时候第一次觉得'这东西真好用'？"

在逐个场景施压之前，先找到产品的**致命弱点**——用户从注册到感受到价值需要几步？每一步都可能流失。

推演要点：
- 产品的 Aha Moment 是什么？（第一次成功创建表单？第一次看到 3D 效果？第一次收到预约？）
- 用户需要经过哪些步骤才能到达 Aha Moment？
- 每一步的流失风险是什么？（太复杂？太慢？不知道该做什么？）
- 有没有"假的 Aha Moment"——用户以为成功了但其实没有？（比如提交了表单但没设置通知，以为没人会看到）

从代码中找什么：
- 找到产品核心价值对应的代码路径（创建→展示→结果反馈）
- 检查这条路径上有多少步，每步有多少需要用户做决策的点
- 检查每步之间有没有流失风险（需要等待、需要跳转、需要额外配置）

提问示例（以一个表单构建器为例）：
- "Aha Moment 应该是'收到第一个表单回复'。但从创建表单到收到回复需要：创建→添加字段→发布→分享链接→等待别人填写。5 步里最后一步不可控——如果用户分享后没人填，就永远到不了 Aha Moment"
- "代码里发布表单后只显示了链接，没有提示'把这个链接发给你的同事'——用户可能不知道下一步该做什么"

---

## 场景 1：首次见面 — "5 秒内看不懂就走人"

推演要点：
- 用户从哪里知道这个产品？（朋友推荐 / 搜索引擎 / 社交媒体 / 应用商店）
- 打开后的第一屏展示了什么？用户能在 5 秒内理解产品用途吗？
- 有没有引导流程？如果有，是帮助用户快速上手还是增加负担？
- 如果没有引导，用户靠自己探索的话，第一步会点哪里？

从代码中找什么：
- 读取首页/入口页面的代码，看 UI 元素的排列和文案
- 读取路由配置，看有没有 onboarding/引导页面
- 读取 CSS/样式代码，看信息层级是否清晰

提问示例（以一个待办事项应用为例）：
- "首页打开后顶部是 'My Tasks' 标题，下面是空列表，底部有个 '+' 按钮。但没有任何文字告诉用户'点击 + 创建你的第一个任务'——一个第一次用的人会以为这个页面是坏的"
- "代码里有 `/onboarding` 路由，但路由守卫里没有强制跳转逻辑，用户可以直接跳过引导直接进入空页面"

---

## 场景 2：核心流程 — "每一步都可能卡住"

推演要点：
- 用户要完成的核心任务是什么？（比如：创建一个笔记、下一笔订单、发送一条消息）
- 完成这个任务需要几步？每一步用户需要做什么决策？
- 在每一步，用户看到的反馈是什么？（成功提示？加载动画？什么都没有？）
- 有没有可以"卡住"的点？（需要用户做选择但不知道选什么、需要输入但不知道格式、需要等待但没有进度提示）

从代码中找什么：
- 读取核心交互的代码（表单提交、按钮点击事件、API 调用），还原完整的操作链路
- 读取状态管理代码，看每一步的 loading/success/error 状态是否都有处理
- 读取 UI 渲染代码，看用户在等待时看到什么

提问示例（以一个在线简历生成器为例）：
- "用户填完所有信息后点击'生成简历'，代码里调用了 `/api/generate` 接口，但页面上只有一个 disabled 的按钮变 enabled，没有 loading 状态——用户点击后不知道是在处理中还是没反应，可能会重复点击"
- "生成完成后代码直接 `window.open(pdfUrl)` 在新标签页打开 PDF，但没有提示'简历已生成'——用户可能根本没注意到新标签页弹出来了"
- "如果生成过程中网络断了，API 调用失败后代码只 `console.error(err)` 但页面没有任何反馈，用户会一直看着空白页面不知道发生了什么"

---

## 场景 3：重复使用 — "第三次就开始烦了"

推演要点：
- 第一次用和第五十次用的体验有什么不同？
- 随着数据积累，列表会不会变得很长？加载会不会变慢？
- 有没有快捷操作？老用户每次都要走完整流程吗？
- 有没有重复性劳动可以自动化？

从代码中找什么：
- 读取列表渲染代码，看有没有分页/虚拟滚动/搜索筛选
- 读取数据加载逻辑，看是一次性加载还是增量加载
- 读取快捷键/批量操作相关代码（如果有的话）

提问示例（以一个书签管理工具为例）：
- "书签列表用 `map()` 全量渲染，没有分页也没有虚拟滚动——用户加到 200 个书签后每次打开页面都会卡 2 秒"
- "没有搜索和筛选功能，用户找一个书签只能手动滚动列表——50 个以内还行，100 个以上就很痛苦了"
- "每次添加书签都要手动选文件夹，没有'最近使用的文件夹'记忆功能，高频用户每天要重复选 10 次"

---

## 场景 4：中途离开 — "回来后一切还在吗？"

推演要点：
- 用户在操作过程中被打断（接电话、开会、切换到其他 app），回来后能继续吗？
- 正在编辑的内容会不会丢失？
- 页面状态（滚动位置、展开的菜单、打开的弹窗）能不能恢复？
- 如果是多步骤流程，能从断点继续还是从头开始？

从代码中找什么：
- 读取数据持久化逻辑，看有没有 localStorage/sessionStorage 保存
- 读取路由和状态管理，看刷新页面后状态是否保留
- 读取表单组件，看有没有草稿自动保存

提问示例（以一个表单构建器为例）：
- "用户正在拖拽构建表单，拖了 20 个字段后浏览器崩溃了——代码里没有自动保存草稿，刷新页面后所有工作都丢了"
- "多步骤表单（基本信息→教育背景→工作经历），用户在第三步填到一半切出去了，回来后页面回到第一步——没有保存中间状态"
- "用户在弹窗里编辑了一个字段的属性，点了取消关闭弹窗，但之前在主页面的滚动位置丢了，要重新滚到对应位置"

---

## 场景 5：数据膨胀 — "100 条没事，10000 条呢？"

推演要点：
- 用户持续使用这个产品，数据量会怎么增长？
- 100 条数据和 10000 条数据的体验差距有多大？
- 有没有数据归档/清理机制？
- 数据导出/备份功能是否完善？

从代码中找什么：
- 读取数据库 Schema 或数据模型，看字段设计是否考虑了扩展
- 读取查询代码，看有没有索引、分页、缓存
- 读取导出/备份相关代码（如果有的话）

提问示例（以一个日记应用为例）：
- "日记数据存在本地 IndexedDB 里，代码里用 `getAll()` 一次性读取全部日记——100 篇以内没问题，1000 篇以上页面会卡死"
- "没有数据导出功能，用户想换个应用或者备份数据只能手动一篇篇复制"
- "日记按时间倒序排列，但没有'按月/按年'的分组视图，半年后用户要找 3 个月前的日记只能慢慢翻"

---

## 场景 6：故意搞破坏 — "用户一定会搞砸的"

推演要点：
- 用户误操作（误删、误改、误提交）后能撤销吗？
- 遇到错误时看到的是什么？友好的提示还是技术报错？
- 输入了产品没预期的内容（超长文本、特殊字符、空值）会怎样？
- 网络异常/服务不可用时用户看到什么？

从代码中找什么：
- 读取错误处理代码（try/catch、.catch()、error boundary），看错误时用户看到什么
- 读取输入校验代码，看哪些字段没有校验
- 读取删除/修改操作的确认逻辑

提问示例（以一个文件管理器为例）：
- "删除文件时代码直接调用 `DELETE /api/files/:id`，没有回收站机制——用户误删后无法恢复，这在文件管理器里是致命的"
- "文件上传失败时 catch 块里只 `alert('上传失败')`，没有告诉用户失败原因（文件太大？格式不对？网络问题？），也没有重试按钮"
- "用户上传了一个 0 字节的空文件，代码没有做校验，文件列表里会出现一个大小为 0 的文件，用户打开时会看到空白页"

---

## 场景 7：产品外的体验 — "用户离开后你还在管吗？"

用户不总是待在你的产品里。他们提交了表单后会关掉页面，预约了会议后会去看邮件。**产品外的通知链路是用户体验的延伸，但经常被忽略。**

推演要点：
- 用户在产品里完成了一个动作（提交、预约、下单），产品外会触发什么通知？
- 通知的内容是什么？格式专业吗？信息完整吗？
- 通知里有没有下一步行动的入口？（reschedule 链接、查看回复按钮）
- 用户收到通知后能不能直接操作，还是要回到产品里？

从代码中找什么：
- 找到邮件/通知发送的代码（sendEmail、notification trigger）
- 检查通知模板的 HTML/文本内容
- 检查通知里有没有包含可操作的链接
- 检查有没有推送通知（Web Push、短信）的集成

提问示例（以一个日程调度工具为例）：
- "用户被预约后收到的邮件只说'你有一个新的预约'，但没有显示预约者的姓名和会议主题——用户要打开产品才知道是谁约的"
- "预约者收到确认邮件后想改时间，邮件里只有一个'取消预约'链接，没有'reschedule'链接——要改时间只能取消重来"
- "代码里有 Web Push 的依赖（`web-push`），但没有注册 Service Worker——浏览器推送通知功能其实没有接通"

---

## 场景 8：换个入口 — "手机上还是同一个产品吗？"

同一个产品，用户可能从不同设备、不同入口进入。**每个入口的体验可能完全不同。**

推演要点：
- 产品有哪些入口？（网页、手机浏览器、PWA、邮件链接、嵌入 iframe、API 调用）
- 不同入口的体验一致吗？
- 从一个入口开始的操作能在另一个入口继续吗？
- 嵌入场景（iframe/组件嵌入）的表现如何？

从代码中找什么：
- 检查响应式设计代码（媒体查询、viewport 设置）
- 检查有没有 PWA 配置（manifest.json、Service Worker）
- 检查有没有深链接/分享链接的路由处理
- 检查 iframe 嵌入时的样式隔离和通信机制

提问示例（以一个表单构建器为例）：
- "用户在电脑上创建了表单，用手机打开分享链接——手机端的编辑器布局错乱，拖拽功能不能用（触摸事件没处理）"
- "表单嵌入到第三方网站时用了 iframe，但表单的弹窗（modal）溢出了 iframe 边界——用户点击提交后弹窗被截断"
- "用户从邮件里的链接打开表单，填到一半想在电脑上继续——但没有登录态同步，手机上是匿名访问，换电脑就找不到之前的填写进度"
